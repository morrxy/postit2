<%= render partial: 'shared/form_errors', locals: {ivar: @post} %>

<%= form_for @post do |f| %>
  <%= f.label :Title %>
  <%= f.text_field :title %>
  <br>

  <%= f.label :URL %>
  <%= f.text_field :url %>
  <br>

  <%= f.label :Description %>
  <%= f.text_area :description %>
  <br>

<!--   <select name="post[category_ids][]" id="" multiple='multiple'>
    <option value="1">option 1</option>
    <option value="2">option 2</option>
  </select>
  <br>
 -->
  <%= f.collection_check_boxes :category_ids, Category.all, :id, :name do |cb| %>
    <% cb.label(class: "checkbox inline") {cb.check_box(class: "checkbox") + cb.text} %>
  <% end %>

  <br>
  <%= f.submit "Create Post", class: 'btn btn-primary' %>
<% end %>

<p>begin test</p>

<%= select_tag(:city_id, '<option value="1">Lisbon</option><option selected="selected">sdfds</option>'.html_safe) %>
<%= select_tag(:city_id, options_for_select([['lisbon', 1], ['madrid', 2]], 2)) %>

