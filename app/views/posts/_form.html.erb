<%= render partial: 'shared/form_errors', locals: {ivar: @post} %>

<%= form_for @post do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <br>

  <%= f.label :url %>
  <%= f.text_field :url %>
  <br>
  <%= f.submit "Create Post", class: 'btn btn-primary' %>
<% end %>
