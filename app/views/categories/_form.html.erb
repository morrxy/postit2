<%= render partial: 'shared/form_errors', locals: {ivar: @category} %>

<%= form_for @category do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <br>
  <%= f.submit 'Create Category', class: 'btn btn-primary' %>
<% end %>
